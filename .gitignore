# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Streamlit
.streamlit/secrets.toml

# OS / 編輯器
.DS_Store
Thumbs.db
.vscode/
.idea/

# 暫存與輸出
*.log
*.tmp
~$*
*_前期評估報告.md
*_逐層表.csv

# 內部文件（含真實財務/案件資料，禁止上傳）
供py/
*知識庫.md
都更/
開發評估流程_SOP*.md
# 註：原本這裡有 blanket `*.html`（沿自 RE-DCF-Tool，用來擋含真實資料的內部 HTML 報告）。
# 但那條會把 apps/web 靜態站的頁面全部排除，導致 GitHub Pages 沒有 index.html→404。
# 已移除。HTML 的資料安全改由 CI Gate 0（check_no_real_names.sh 掃全檔含 HTML）把關。
/local_calibration/
